# Optional: public API base URL for production builds (no trailing slash).
# VITE_API_BASE_URL=https://api.example.com

# Dev only: where Vite proxies /api and POST /contact (server-side).
# VITE_API_PROXY_TARGET=http://127.0.0.1:8080
