# Root / Docker Compose (optional). Prefer per-app .env in apps/web and apps/api.

# Example: public API URL for production builds of the web app
# VITE_API_BASE_URL=https://api.example.com

# Compose runs from infra/docker; see apps/api/.env.example for API variables.
